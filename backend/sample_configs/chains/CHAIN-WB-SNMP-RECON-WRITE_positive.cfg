;; Whitebox unrestricted reachability + default creds (minimal fixture: only this chain)
[system.core]
device_identifier = "wb-snmp-pos"
session_idle_limit = 10
aaa new-model

[security.credentials]
password_encryption = "enabled"
local_operator = "admin:scrypt:hash12345"

[network.port.eth0]
address = "10.60.0.1/24"
management_protocols = ["ssh"]